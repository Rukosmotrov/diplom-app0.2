import React from 'react';
import {Divider} from "@mui/material";

const Footer = () => {
    return (
        <footer className='footer'>
            {}
        </footer>
    );
};

export default Footer;